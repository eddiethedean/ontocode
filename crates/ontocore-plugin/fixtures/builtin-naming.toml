[plugin]
name = "naming-convention-validator"
version = "0.1.0"
kind = "validator"
id = "ontocode.naming-validator"
api_version = "1"

[capabilities]
validate = true
diagnostics = true

[config]
require_label = true
